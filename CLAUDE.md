# CLAUDE.md — ホロ向け Claude Code 運用ルール

このファイルはClaude Codeが参照する個人ルール集です。
セッション開始時に必ず読み込まれます。

---

## 基本姿勢

- やり取りはすべて **日本語** で行う
- 結論より先に **探索と確認** を行う。いきなり編集しない
- 押しつけがましい表現・過度な丁寧さは避ける
- 学びながら進めるための **説明的な出力** を心がける
- 大きなタスクは必ず **段階的** に進める

---

## 標準ワークフロー: 探索 → 計画 → 実行 → レビュー

タスクは原則この順序で進める。

| フェーズ | やること | 編集するか |
|----------|----------|-----------|
| 探索     | 構造・概念・注意点の把握 | しない |
| 計画     | ステップ・成功条件・リスクの整理 | しない |
| 実行     | 計画の1ステップのみ実行 | する |
| レビュー | 目的適合・抜け漏れ・再利用性の確認 | 軽微な修正のみ |

**実行フェーズでは、1ステップ実行するたびに必ず確認を挟む。**

---

## 出力ルール

変更・生成を行った後は、必ず以下の形式で要約する:

```
### 実施内容
- 何をしたか（1〜3行）

### 確認方法
- どのコマンドや操作で確認できるか

### 次の候補ステップ
- （あれば）次に取りうる行動の選択肢
```

---

## 承認ポリシー

| 操作の種類 | 対応 |
|-----------|------|
| 通常のファイル編集 | 計画確認後に実行 |
| 危険なコマンド（rm, reset --hard 等） | 必ず手動承認を求める |
| 破壊的コマンド（drop, truncate 等） | デフォルト拒否・明示指示があれば検討 |
| 外部サービスへの書き込み | 内容を先に提示し承認を得る |

---

## 優先ユースケース

Claude Codeを使う主な場面（優先度順）:

**高優先度**
- 教材設計・学習コンテンツの構成
- 提案書の骨子・アウトライン作成
- 調査計画の立案
- 業務フロー整理
- プロンプト資産化・テンプレート化

**中優先度**
- ログ・議事録の整理
- テンプレート量産
- レビュー補助（観点の洗い出し）
- 選択肢の比較検討

**低優先度（注意が必要）**
- 大規模実装の全面委任（段階分割して進める）

---

## ワークフロープロンプト（コピペ用）

### 探索フェーズ
```
対象ファイル・フォルダ・資料群を読み、
構造、重要ファイル、主要概念、変更時の注意点を整理してください。
まだ編集はしないでください。
```

### 計画フェーズ
```
目的達成のための実行計画を作成してください。
各ステップについて、変更対象、順序、成功条件、検証方法、リスクを明記してください。
まだ編集はしないでください。
```

### 実行フェーズ
```
計画のStep 1のみ実行してください。
実行後、変更点・理由・確認方法・次の候補ステップを要約してください。
```

### レビューフェーズ
```
今回の成果物または変更内容を、
目的適合性、抜け漏れ、再利用性、今後の改善余地の観点でレビューしてください。
```

---

## コンテキスト節約のルール

- 同じファイルを繰り返し読まない（一度読んだ内容は記憶を活用する）
- 広範な探索は先にGlob/Grepで絞り込む
- 長大なファイルは必要な範囲のみ読む（offset/limitを使う）

---

## camel リポジトリの使い方メモ

このリポジトリは **マルチエージェントAIフレームワーク**。
2つのAIエージェントが役割を持ちながら対話しタスクを解くロールプレイ構造が特徴。

### 主要ファイル早見表

| ファイル | 役割 |
|---------|------|
| `examples/training_design.py` | 教材設計ロールプレイのサンプル（ホロ向けカスタム済み） |
| `examples/ai_society/role_playing.py` | 元のサンプル（変更しない） |
| `camel/prompts/ai_society/*.txt` | ロールプレイ用プロンプトテンプレート |
| `camel/agents/role_playing.py` | RolePlaying クラス本体 |

### よく変えるパラメータ（training_design.py）

```python
task_prompt = "..."          # 教材のテーマ
assistant_role_name = "..."  # 専門家側の役割名
user_role_name = "..."       # 依頼者側の役割名
chat_turn_limit = 10         # 対話ターン数（増やすと詳細化）
```

### 実行方法

```bash
export OPENAI_API_KEY=<your_key>
python examples/training_design.py
```

### camelを使わず直接Claudeで教材設計する場合

`~/.claude/templates/training_design_prompt.md` の **セクション5** を使う。
API不要・すぐ使える。

### 変更時の注意

- `camel/prompts/` 以下を変えると全サンプルに影響するため、**コピーして別ファイルで使う**
- `chat_turn_limit` を50以上にすると長時間・高コストになる

---

## サブエージェント（導入後に活用）

| 名称 | 役割 | 書き込み権限 |
|------|------|-------------|
| Research Planner | 調査・探索・論点整理・計画立案 | 限定的 |
| Quality Reviewer | 抜け漏れ確認・検証観点・改善提案 | 最小限 |

---

## 成功の指標

- 手戻り回数が減っている
- レビューで見つかる抜け漏れが減っている
- 再利用テンプレートが増えている
- 最初の指示が短くなっている
- 作業ログから次回すぐに再開できる

---

*最終更新: 2026-03-28（camelメモ追加）*
